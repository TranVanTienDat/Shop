//selector comment
// export const commentSelector = (state) => state.comment;

//selector infoUser
export const userData = (state) => state.userData;

// select my cart
export const myCart = (state) => state.myCart;

//select loading
export const setGlobalLoading = (state) => state.setGlobalLoading;

// search params
export const searchParams = (state) => state.searchParams;
// voucher
export const voucherSelector = (state) => state.voucher;

// buy product
export const buySelector = (state) => state.buyProduct;
